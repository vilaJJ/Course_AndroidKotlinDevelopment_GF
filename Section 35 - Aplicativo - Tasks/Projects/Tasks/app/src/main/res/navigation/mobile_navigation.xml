<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_all_tasks">

    <fragment
        android:id="@+id/nav_all_tasks"
        android:name="dev.mandevilla.tasks.view.AllTasksFragment"
        android:label="@string/all_tasks"
        tools:layout="@layout/fragment_all_tasks" />

    <fragment
        android:id="@+id/nav_next_tasks"
        android:name="dev.mandevilla.tasks.view.NextTasksFragment"
        android:label="@string/next_7_days"
        tools:layout="@layout/fragment_next_tasks" />

    <fragment
        android:id="@+id/nav_expired"
        android:name="dev.mandevilla.tasks.view.ExpiredTasksFragment"
        android:label="@string/expired"
        tools:layout="@layout/fragment_expired_tasks" />

</navigation>